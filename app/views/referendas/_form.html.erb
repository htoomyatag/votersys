<%= form_with(model: referenda) do |form| %>
  <% if referenda.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(referenda.errors.count, "error") %> prohibited this referenda from being saved:</h2>

      <ul>
        <% referenda.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
